import { getAuth, onAuthStateChanged, signInWithRedirect, GoogleAuthProvider } from "firebase/auth";
import { getApps, getApp, initializeApp } from "firebase/app";
import "./styles.css";

type Membership = { organization_id: string; status: string; permissions?: string[] };
type Session = { sessionToken: string; memberships: Membership[] };
type Profile = { organization_id: string; jurisdiction_name?: string; applicability_status: string };
type Period = { id: string; period_start: string; period_end: string; reporting_basis: string; status: string; external_submission_status: string };
type Workspace = { profiles: Profile[]; periods: Period[]; wasteReports: unknown[]; eprReports: unknown[]; esgReports: unknown[] };

const config = { apiKey: import.meta.env.VITE_FIREBASE_API_KEY, authDomain: import.meta.env.VITE_FIREBASE_AUTH_DOMAIN, projectId: import.meta.env.VITE_FIREBASE_PROJECT_ID, appId: import.meta.env.VITE_FIREBASE_APP_ID };
const app = getApps().some((item) => item.name === "bwg-reporting") ? getApp("bwg-reporting") : initializeApp(config, "bwg-reporting");
const auth = getAuth(app);
const root = document.getElementById("bwg-reporting");
if (!root) throw new Error("BWG reporting mount missing");
const mount = root;

let session: Session | null = null;
let workspace: Workspace = { profiles: [], periods: [], wasteReports: [], eprReports: [], esgReports: [] };
let message = "";
let error = "";
const escapeHtml = (value: unknown) => String(value ?? "").replace(/[&<>\"]/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "\"": "&quot;" }[c] ?? c));
const api = async (path: string, options: RequestInit = {}) => { const response = await fetch(path, { ...options, headers: { "Content-Type": "application/json", ...(options.headers ?? {}), ...(session ? { Authorization: `Bearer ${session.sessionToken}` } : {}) } }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error ?? `Request failed (${response.status})`); return body; };
const activeMembership = () => session?.memberships.find((m) => m.status === "VERIFIED") ?? null;
const canWrite = (permission: string) => activeMembership()?.permissions?.includes(permission) ?? false;
const value = (id: string) => (document.getElementById(id) as HTMLInputElement | null)?.value.trim() ?? "";
const numberValue = (id: string) => Number(value(id));

function render(): void {
  const profile = workspace.profiles[0];
  const activePeriod = workspace.periods[0];
  const periodOptions = workspace.periods.map((p) => `<option value="${escapeHtml(p.id)}">${escapeHtml(p.period_start)} → ${escapeHtml(p.period_end)} · ${escapeHtml(p.reporting_basis)}</option>`).join("") || `<option value="">Create a period first</option>`;
  mount.innerHTML = `<section class="compliance-card bwg-card" aria-labelledby="bwg-title">
    <div class="compliance-head"><div><p class="eyebrow">BULK WASTE GENERATOR</p><h2 id="bwg-title">EPR &amp; ESG reporting</h2><p>Record authoritative BWG applicability, waste flows, EPR evidence and ESG metrics. Submission or acceptance is never inferred.</p></div><span class="compliance-state">${session ? "AUTHENTICATED" : "SIGN IN REQUIRED"}</span></div>
    ${message ? `<div class="resource-flow-state">${escapeHtml(message)}</div>` : ""}${error ? `<div class="resource-flow-state" role="alert">${escapeHtml(error)}</div>` : ""}
    ${!session ? `<button id="bwg-sign-in" type="button">Sign in to BWG workspace</button>` : !activeMembership() ? `<div class="field-help">A verified organization membership is required.</div>` : `<div class="bwg-grid">
      <form id="bwg-profile-form" class="esg-form"><h3>1. BWG applicability</h3><label>Jurisdiction ID<input id="bwg-jurisdiction" required placeholder="Authorized geography UUID"></label><label>Establishment type<input id="bwg-type" required placeholder="Office, hotel, hospital, campus…"></label><div class="esg-form__row"><label>Floor area (sq.m.)<input id="bwg-area" required type="number" min="0" step="0.01"></label><label>Water (L/day)<input id="bwg-water" required type="number" min="0" step="0.01"></label></div><label>Waste generation (kg/day)<input id="bwg-waste" required type="number" min="0" step="0.01"></label><button type="submit" ${canWrite("profile:update") ? "" : "disabled"}>Save authoritative profile</button>${profile ? `<p class="field-help">Current status: <strong>${escapeHtml(profile.applicability_status)}</strong> · ${escapeHtml(profile.jurisdiction_name ?? "authorized jurisdiction")}</p>` : ""}</form>
      <form id="bwg-period-form" class="esg-form"><h3>2. Reporting period</h3><div class="esg-form__row"><label>Start<input id="bwg-start" required type="date"></label><label>End<input id="bwg-end" required type="date"></label></div><label>Reporting basis<select id="bwg-basis"><option value="SWM_2026">SWM Rules 2026</option><option value="EPR">EPR</option><option value="ESG">ESG</option><option value="BRSR_VALUE_CHAIN">BRSR / value chain (where applicable)</option></select></label><button type="submit" ${canWrite("waste:record") ? "" : "disabled"}>Create reporting period</button>${activePeriod ? `<p class="field-help">Latest: ${escapeHtml(activePeriod.period_start)} → ${escapeHtml(activePeriod.period_end)} · ${escapeHtml(activePeriod.status)} · external submission: ${escapeHtml(activePeriod.external_submission_status)}</p>` : ""}</form>
      <form id="bwg-waste-form" class="esg-form"><h3>3. Waste report</h3><label>Reporting period<select id="bwg-waste-period" required>${periodOptions}</select></label><label>Waste stream<input id="bwg-stream" required placeholder="Dry / wet / plastic / organic…"></label><div class="esg-form__row"><label>Generated<input id="bwg-generated" required type="number" min="0" step="0.001"></label><label>Segregated<input id="bwg-segregated" type="number" min="0" step="0.001" value="0"></label></div><div class="esg-form__row"><label>Channelised<input id="bwg-channelized" type="number" min="0" step="0.001" value="0"></label><label>Processed<input id="bwg-processed" type="number" min="0" step="0.001" value="0"></label></div><label>Unit<input id="bwg-unit" required value="kg"></label><label>Verified evidence UUID <span class="esg-form__optional">optional</span><input id="bwg-waste-evidence"></label><label>Approved verification UUID <span class="esg-form__optional">optional</span><input id="bwg-waste-verification"></label><button type="submit" ${canWrite("waste:record") && workspace.periods.length ? "" : "disabled"}>Record waste report</button></form>
      <form id="bwg-epr-form" class="esg-form"><h3>4. EPR report</h3><label>Reporting period<select id="bwg-epr-period" required>${periodOptions}</select></label><label>EPR scheme UUID<input id="bwg-scheme" required placeholder="Authoritative epr_schemes UUID"></label><label>Category code<input id="bwg-category" required placeholder="Authoritative category code"></label><div class="esg-form__row"><label>Obligated quantity<input id="bwg-obligated" required type="number" min="0" step="0.001"></label><label>Fulfilled quantity<input id="bwg-fulfilled" required type="number" min="0" step="0.001"></label></div><label>Verified evidence UUID <span class="esg-form__optional">optional</span><input id="bwg-epr-evidence"></label><label>Approved verification UUID <span class="esg-form__optional">optional</span><input id="bwg-epr-verification"></label><button type="submit" ${canWrite("epr:manage") && workspace.periods.length ? "" : "disabled"}>Record EPR report</button><p class="field-help">This records an authoritative report only. It does not submit to CPCB or claim CPCB acceptance.</p></form>
      <form id="bwg-esg-form" class="esg-form"><h3>5. ESG metric</h3><label>Reporting period<select id="bwg-esg-period" required>${periodOptions}</select></label><label>Metric code<input id="bwg-metric" required placeholder="Authoritative metric code"></label><div class="esg-form__row"><label>Scope<input id="bwg-scope" required placeholder="1 / 2 / 3 / IMPACT"></label><label>Unit<input id="bwg-esg-unit" required placeholder="kg, kWh, tCO2e…"></label></div><label>Value<input id="bwg-value" required type="number" min="0" step="any"></label><label>Verified evidence UUID <span class="esg-form__optional">optional</span><input id="bwg-esg-evidence"></label><label>Approved verification UUID <span class="esg-form__optional">optional</span><input id="bwg-esg-verification"></label><button type="submit" ${canWrite("reports:write") && workspace.periods.length ? "" : "disabled"}>Record ESG metric</button><p class="field-help">Only users with the authoritative ESG write capability can record metrics.</p></form>
    </div><div class="compliance-list"><h3>Authoritative records</h3><p class="field-help">Profiles: ${workspace.profiles.length} · periods: ${workspace.periods.length} · waste reports: ${workspace.wasteReports.length} · EPR reports: ${workspace.eprReports.length} · ESG reports: ${workspace.esgReports.length}</p></div>`;
  mount.querySelector("#bwg-sign-in")?.addEventListener("click", () => void signInWithRedirect(auth, new GoogleAuthProvider()));
  mount.querySelector("#bwg-profile-form")?.addEventListener("submit", (event) => { event.preventDefault(); void saveProfile(); });
  mount.querySelector("#bwg-period-form")?.addEventListener("submit", (event) => { event.preventDefault(); void createPeriod(); });
  mount.querySelector("#bwg-waste-form")?.addEventListener("submit", (event) => { event.preventDefault(); void saveWaste(); });
  mount.querySelector("#bwg-epr-form")?.addEventListener("submit", (event) => { event.preventDefault(); void saveEpr(); });
  mount.querySelector("#bwg-esg-form")?.addEventListener("submit", (event) => { event.preventDefault(); void saveEsg(); });
}

async function load(): Promise<void> { if (!session) return; const response = await api("/api/v1/workspaces/bwg"); workspace = response.data ?? workspace; render(); }
async function write(path: string, body: unknown): Promise<void> { error = ""; message = ""; try { await api(path, { method: "POST", body: JSON.stringify(body) }); message = "Saved to PostgreSQL authoritatively."; await load(); } catch (caught) { error = caught instanceof Error ? caught.message : "Authoritative write failed."; render(); } }
async function saveProfile() { await write("/api/v1/bwg/profile", { organizationId: activeMembership()?.organization_id, jurisdictionId: value("bwg-jurisdiction"), establishmentType: value("bwg-type"), floorAreaSqm: numberValue("bwg-area"), waterConsumptionLpd: numberValue("bwg-water"), wasteGenerationKgDay: numberValue("bwg-waste") }); }
async function createPeriod() { await write("/api/v1/bwg/reporting-periods", { organizationId: activeMembership()?.organization_id, periodStart: value("bwg-start"), periodEnd: value("bwg-end"), reportingBasis: (document.getElementById("bwg-basis") as HTMLSelectElement)?.value }); }
async function saveWaste() { const id = (document.getElementById("bwg-waste-period") as HTMLSelectElement)?.value ?? ""; await write(`/api/v1/bwg/reporting-periods/${encodeURIComponent(id)}/waste`, { wasteStream: value("bwg-stream"), generatedQuantity: numberValue("bwg-generated"), segregatedQuantity: numberValue("bwg-segregated"), channelizedQuantity: numberValue("bwg-channelized"), processedQuantity: numberValue("bwg-processed"), unit: value("bwg-unit"), evidenceId: value("bwg-waste-evidence") || undefined, verificationId: value("bwg-waste-verification") || undefined }); }
async function saveEpr() { const id = (document.getElementById("bwg-epr-period") as HTMLSelectElement)?.value ?? ""; await write(`/api/v1/bwg/reporting-periods/${encodeURIComponent(id)}/epr`, { schemeId: value("bwg-scheme"), categoryCode: value("bwg-category"), obligatedQuantity: numberValue("bwg-obligated"), fulfilledQuantity: numberValue("bwg-fulfilled"), evidenceId: value("bwg-epr-evidence") || undefined, verificationId: value("bwg-epr-verification") || undefined }); }
async function saveEsg() { const id = (document.getElementById("bwg-esg-period") as HTMLSelectElement)?.value ?? ""; await write(`/api/v1/bwg/reporting-periods/${encodeURIComponent(id)}/esg`, { metricCode: value("bwg-metric"), scope: value("bwg-scope"), unit: value("bwg-esg-unit"), value: numberValue("bwg-value"), evidenceId: value("bwg-esg-evidence") || undefined, verificationId: value("bwg-esg-verification") || undefined }); }

onAuthStateChanged(auth, async (user) => { if (!user) { session = null; workspace = { profiles: [], periods: [], wasteReports: [], eprReports: [], esgReports: [] }; render(); return; } try { if (!user.emailVerified) throw new Error("Verified email is required before BWG reporting access."); const idToken = await user.getIdToken(true); const exchanged = await api("/api/v1/auth/exchange", { method: "POST", body: JSON.stringify({ idToken }) }); session = { sessionToken: exchanged.sessionToken, memberships: exchanged.memberships ?? [] }; const me = await api("/api/v1/auth/me"); session.memberships = me.memberships ?? []; await load(); } catch (caught) { session = null; error = caught instanceof Error ? caught.message : "BWG workspace unavailable."; render(); } });
render();

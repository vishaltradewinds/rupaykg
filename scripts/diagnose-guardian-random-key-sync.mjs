import fs from 'node:fs';const base='http://127.0.0.1:3000/api/v1';const pass=fs.readFileSync('scripts/import-wa03-local.mjs','utf8').match(/GUARDIAN_SR_PASSWORD\|\|'([^']+)'/)?.[1];const l=await fetch(base+'/accounts/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:'StandardRegistry',password:pass,role:'STANDARD_REGISTRY'})});const lr=await l.json();const t=await fetch(base+'/accounts/access-token',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({refreshToken:lr.refreshToken})});const tr=await t.json();const a=tr.accessToken||tr.token;const r=await fetch(base+'/demo/random-key',{headers:{authorization:'Bearer '+a}});const j=await r.json();console.log('HTTP='+r.status);console.log('KEYS='+Object.keys(j).join(','));console.log('CRED_KEYS='+Object.keys(j.hederaCredentials||{}).join(','));